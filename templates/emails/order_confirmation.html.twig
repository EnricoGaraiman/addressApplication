<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Order confirmation</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
<div class="container">
    <h1>ORDER CONFIRMATION</h1>
    <p>Thank you for your order!</p>
    <p>We've receiver your order and will contact you as soon as your package is shipped.
        You can find your purchase information below:</p>
    <br>
    <h1>Summary</h1>
    {% set i = 0 %}
    <div class="table-responsive">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Product</th>
                <th scope="col">Description</th>
                <th scope="col">Quantity</th>
                <th scope="col">Price</th>
            </tr>
            </thead>
            <tbody>
            {% for product in products %}
                {% set i = i + 1 %}
                <tr>
                    <td>{{ i }}</td>
                    <td>{{ product.product.name }}</td>
                    <td>{{ product.product.description }}</td>
                    <td>{{ product.qty }}</td>
                    <td>{{ product.product.price * product.qty }}</td>
                </tr>
            {% endfor %}
            </tbody>
            <tfoot>
            <tr>
                <td></td>
                <td></td>
                <td></td>
                <td class="fw-bold">{{ numberOfProducts }} products</td>
                <td class="fw-bold">{{ totalPrice }} lei</td>
            </tr>
            </tfoot>
        </table>
    </div>
    <p>Address: {{ destinationAddress.city.country }}, {{ destinationAddress.city }}
        , {{ destinationAddress.address }}</p>
    <br>
    <p>Thank you,</p>
    <p>AddressApplication Team</p>
</div>
</body>
</html>