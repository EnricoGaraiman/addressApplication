<div class="black-bg">
    {{ form_start(form) }}
    <h1>Add a new address</h1>
    {{ form_row(form.country, {'attr': {'class': 'form-control'}}) }}
    {{ form_row(form.city, {'attr': {'class': 'form-control'}}) }}
    {{ form_row(form.address, {'attr': {'class': 'form-control'}}) }}
    {{ form_row(form.isDefault, { 'label': 'Set default address',
        'attr': {'class': 'form-check-input mx-2'}}) }}
    <button type="submit" class="btn btn-dark mt-2">Add a new address</button>
    <a href="{{ path('profile') }}" class="btn btn-outline-light mt-2 float-end">Back to profile</a>
    {{ form_end(form) }}
</div>

